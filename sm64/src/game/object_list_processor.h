/* new64 -- placeholder for <game/object_list_processor.h>; see sm64/src/shims/ for the stub
 * implementations of anything hosted code actually calls. */
#ifndef NEW64_OBJECT_LIST_PROCESSOR_H
#define NEW64_OBJECT_LIST_PROCESSOR_H

#include "types.h"

#endif /* NEW64_OBJECT_LIST_PROCESSOR_H */

/* Number of objects currently in the world. The demo keeps only the player. */
extern s16 gPrevFrameObjectCount;
