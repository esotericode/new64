/* new64 -- placeholder for <model_ids.h>. The demo defines no behaviours, dialog,
 * sequences, levels or models; this exists so hosted sources that include it
 * still compile. */
#ifndef NEW64_MODEL_IDS_H
#define NEW64_MODEL_IDS_H

#include "types.h"

#endif /* NEW64_MODEL_IDS_H */
