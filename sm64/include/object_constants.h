/* new64 -- placeholder for <object_constants.h>. The demo defines no behaviours, dialog,
 * sequences, levels or models; this exists so hosted sources that include it
 * still compile. */
#ifndef NEW64_OBJECT_CONSTANTS_H
#define NEW64_OBJECT_CONSTANTS_H

#include "types.h"

#endif /* NEW64_OBJECT_CONSTANTS_H */
